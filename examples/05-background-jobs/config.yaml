server:
  host: 0.0.0.0
  port: 8080
  mode: debug

data:
  database:
    master:
      driver: sqlite3
      source: "file:jobs.db?cache=shared&_fk=1"

worker:
  max_workers: 10
  queue_size: 100
  job_timeout: 300

logger:
  level: debug
  format: json
  output: stdout
